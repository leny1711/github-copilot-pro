# ========================================
# IMPORTANT: Environment variables in Expo
# ========================================
# This file is for reference only. Expo/React Native apps don't use .env files directly.
# The API URL is now configured in src/config/api.config.ts based on the platform:
# - Android Emulator: http://10.0.2.2:3000/api
# - iOS Simulator: http://localhost:3000/api
# - Production: Set your production URL in api.config.ts

# For production deployment, update the API_CONFIG.BASE_URL in:
# mobile/src/config/api.config.ts

# Stripe (if needed - to be configured via app.json or expo-constants)
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here

# Google Maps (configured via app.json)
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
